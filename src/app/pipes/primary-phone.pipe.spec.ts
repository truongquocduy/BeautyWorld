import { PrimaryPhonePipe } from './primary-phone.pipe';

describe('PrimaryPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimaryPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
